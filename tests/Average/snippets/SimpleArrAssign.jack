class SimpleArrAssign {
   function void main() {
     var Array a;
     var int length;
     var int i, sum;

     let length = 7;
     let a = Array.new(length); // constructs the array

     // old test code above, new snippet below
     let i = 0;
     let a[i] = 343;
     let sum = sum + a[i];

     return;
   }
}